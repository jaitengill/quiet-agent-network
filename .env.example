# Postgres — the network agent's own role on contact_intelligence
# (secret: network-agent-contact-intel-dsn; grants in DEPLOY.md §2)
CONTACT_INTEL_DSN=postgres://network_agent:<pw>@10.98.0.3:5432/contact_intelligence?sslmode=require

# Embeddings for firm-contacts semantic search (secret: openai-api-key)
OPENAI_API_KEY=

# Research providers — each optional; a missing key just degrades that slice
EXA_API_KEY=
XAI_API_KEY=
PARALLEL_API_KEY=
X_BEARER_TOKEN=

# Only needed off-VM (on the VM, project comes from metadata)
GCP_PROJECT=atlas-486120
